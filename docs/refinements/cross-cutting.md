# Cross-cutting

This area is the ROADMAP's old catch-all list of unordered cross-cutting deferrals; it has no single origin ADR, and the only ADR cited inside it is [ADR-0022](../adr/ADR-0022-email-write-confirmer.md), attached to the one entry that has landed. Extracted from the ROADMAP's deferred-refinements section on 2026-07-15 with the entries kept verbatim; landed entries are the historical record of what each deferral became, and the index at [index.md](index.md) carries the recommended pickup order.

**Open items:** pointer-input injection, richer memory policies, macOS/Linux OS backends, more subagent roles

**Cross-cutting (originally "Later, unordered"):** pointer-input injection (extend the proto
first), richer memory policies (**the email-write tool landed 2026-07-08 as Slice 8.8**,
ADR-0022), macOS/Linux OS backends, more subagent roles.
