# The partial-degradation policy for the aggregate

**Status:** landed 2026-07-03
**Area:** tools-mcp
**Origin:** [ADR-0009](../../adr/ADR-0009-tools-mcp.md)

The partial-degradation policy for the aggregate, whose degraded-mode addendum adds
`SkipUnavailableToolRegistry` + `CORTEX_TOOLS_ON_UNAVAILABLE=skip`, default `fail`.

This item has no bullet of its own in the area doc. It was recorded in its lead paragraph on the
tools of Slice 6, beside multi-server aggregation, advertised-tool filtering, and
readable-text-from-HTML extraction.
