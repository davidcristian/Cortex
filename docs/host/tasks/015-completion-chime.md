# A soft completion chime

**Status:** never attempted
**Sitting:** overlay-polish
**Capability:** W
**Origin:** [ADR-0011](../../adr/ADR-0011-body-v1.md)

The overlay polish pass is where this decision was recorded, and its part 3 is the morph to a true
screen corner.

**A soft completion chime**, opt-in later or never
([design/overlay-ux.md](../../design/overlay-ux.md) section 9). It is listed there among five open
design decisions, four of which are settled and stale in that doc: the palette is locked to one
gradient, both themes ship, the preview's auto-dismiss and hover-pause landed, and the corner is
part 3 above. The chime is untouched.

## Trail

- 2026-07-12: With this user pass and the one of 2026-07-03, every other line of
  [design/overlay-ux.md](../../design/overlay-ux.md) section 9 was settled, which is what leaves the
  chime as the last genuinely open line there.
- 2026-07-19: When host work was extracted into its own backlog, the chime was recorded among the
  decisions awaiting the user, which are weighed rather than run. Those decisions were listed as
  pointers instead of being copied, so that a decision has exactly one home and so that the pointers
  keep it from being lost when the ROADMAP slims. The rule was written as each decision staying at
  its ADR, while the pointer given for the chime is
  [design/overlay-ux.md](../../design/overlay-ux.md) section 9.
