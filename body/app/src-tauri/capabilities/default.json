{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Overlay window: listen for the host activate event; window ops are called from Rust.",
  "windows": ["overlay"],
  "permissions": ["core:default", "core:event:allow-listen", "core:event:allow-unlisten"]
}
